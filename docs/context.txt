SHOPPER DIGITAL TWIN - PROJECT CONTEXT LOG
==========================================
Purpose of this file: a running, detailed record of every decision, implementation
step and result, so anyone (or any new Claude session) can pick up the project cold.
Newest entries go at the bottom of the LOG section. The SNAPSHOT section is kept current.

------------------------------------------------------------------
SNAPSHOT (current state)
------------------------------------------------------------------
Last updated:      2026-09-25 19:50 UTC
Phase:             Planning complete, implementation not started
Owner:             Manuga
Language:          Python
Domain:            General store, Walmart-style (groceries, household, health & beauty,
                   pet, electronics, clothing, toys, home & garden, books)
Repository:        https://github.com/ManugaHewa/Shopper-Digital-Twin (linked; docs in PR #1)
Next step:         Connect the GitHub repo, then Milestone 1 (synthetic world generator)

Key files (in /mnt/project-files/digital-twin/):
  plan.md                               Detailed build plan and algorithm design
  Shopper_Digital_Twin_Overview.pdf     Project overview (problem, solution, method, roadmap)
  context.txt                           This log

------------------------------------------------------------------
PROJECT SUMMARY
------------------------------------------------------------------
Each simulated customer gets a "twin" learned only from their browsing/purchase logs.
Twins are run forward in a simulator to predict behaviour under changes (price change,
new product, stock-out, promotion). Because the data is generated, every shopper's
true parameters are known (the "answer key"), so what-if forecasts can be scored
against the true outcome. That validation is the portfolio headline.

------------------------------------------------------------------
ARCHITECTURE (planned)
------------------------------------------------------------------
1. Synthetic world   ~5k products, ~100k shoppers, ~10M events; hidden true params saved separately
2. Features          product embeddings (co-view/co-buy), shopper histories, prices, context
3. Twin model        per-shopper multinomial logit choice model:
                       utility = taste . product_embedding + habit + context
                                 - price_sensitivity * price + noise
                     softmax over shown items + an outside "buy nothing" option
                     hierarchical pooling (shrink sparse shoppers toward similar shoppers)
                     optional SASRec-style sequence transformer feeding the context term
                     parameter uncertainty kept for confidence bands
4. Simulator         per day, vectorised over all twins: arrive (Poisson, seasonal) ->
                     browse (top ~50 via FAISS) -> choose (softmax sample) -> update state
5. What-if engine    edit world, re-simulate with SAME random seeds as baseline, diff results
6. Evaluation        HR@10/NDCG@10, parameter recovery, what-if accuracy vs. answer key,
                     calibration (Brier), baselines (popularity, MF, single elasticity), speed
7. Demo              Streamlit: price slider, segment breakdown, "interview a twin" (Claude API,
                     explanation only - never changes the numbers)

Planned stack: NumPy, pandas/Polars, Parquet, PyTorch, scikit-learn, FAISS, Streamlit,
Claude API, pytest.

------------------------------------------------------------------
DECISIONS
------------------------------------------------------------------
2026-09-25  Project idea: shopper digital twin (chosen over intent-from-mouse, life-event
            detector, generative recs, taste map, bandit recommender).
2026-09-25  Language: Python.
2026-09-25  Domain: general Walmart-style store. Reason: staples (groceries, household,
            health, pet) expose repeat-buying cycles and brand loyalty; discretionary
            categories (clothing, electronics, toys, home) expose taste and willingness to
            pay; store-brand vs. name-brand pairs expose price sensitivity. One store
            covers all three behaviours.
2026-09-25  LLM layer is explanation-only; predictions come from the choice model so they
            are reproducible and cheap at 100k-shopper scale.

OPEN QUESTIONS
  - GitHub repo URL (pending from Manuga)
  - Use Claude API for twin interviews? (default: yes, added late in the roadmap)

------------------------------------------------------------------
LOG (chronological, newest last)
------------------------------------------------------------------
[2026-09-25 19:40 UTC] Planning
  - Wrote plan.md: 7-part breakdown, full algorithm description, evaluation plan,
    8 milestones, scale targets, open decisions.

[2026-09-25 19:50 UTC] Domain + documentation
  - Manuga chose a general Walmart-style store; confirmed it supports both taste and
    repeat-buying signals (see DECISIONS).
  - Created Shopper_Digital_Twin_Overview.pdf (6 pages): overview, problem, solution,
    why a general store, how it works (decision rule, learning, simulation, what-ifs,
    AI-agent layer), success metrics, technology, roadmap, scale and limits.
    Generated with Python + reportlab.
  - Created this context.txt; it will be updated after every implementation step.

[2026-09-25 19:52 UTC] Repository
  - Manuga created https://github.com/ManugaHewa/Shopper-Digital-Twin
  - Attaching it failed: Manuga's GitHub account is not linked to Claude yet. Waiting
    on Manuga to connect GitHub and add the repo in Project settings. The PDF has not
    been uploaded to the repo yet.

[2026-09-25 19:55 UTC] Repository linked + docs uploaded
  - Manuga linked GitHub; repo attached and cloned (default branch: main).
  - Branch docs/project-overview: README.md rewritten with project summary; added
    docs/Shopper_Digital_Twin_Overview.pdf, docs/plan.md, docs/context.txt.
  - Opened PR #1: https://github.com/ManugaHewa/Shopper-Digital-Twin/pull/1
  - Convention: this file (/mnt/project-files/digital-twin/context.txt) is the working
    copy; docs/context.txt in the repo is synced with each push.
